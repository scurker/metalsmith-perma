language: node_js
node_js:
- 'node'
- '4'
before_install:
- npm install -g coveralls
script:
# - npm run lint
- npm run test
sudo: false
cache:
  directories:
  - node_modules
after_success:
  - npm run coverage
  - ./node_modules/.bin/nyc report --reporter=text-lcov | coveralls